# V Ai Trader (ALFA AI Integration)

Quick local setup to run the FastAPI backend and static frontend.

Defaults:
- Admin user: `Anthony Jones`
- Password: `Timeline@337`

Run locally:

```bash
cd v_ai_trader
python -m pip install -r requirements.txt
./run.sh
```

Open http://localhost:8000 in your browser. Log in with the default admin account, open Admin → Deriv Security to paste your Deriv token, then connect.
